---
source: content/toolkit
clean: false
frontmatter: true
metadata:
  site:
    title: SAFETAG
    baseurl: '/SAFETAG'
plugins:
  # This needs to:
  #  - deploy to the workspace folder with symlinks for multi parents
  #  - and deploy taxonomy views also with synlinks
  #  - and
  - metalsmith-transclude:
      conmments: true
  - metalsmith-debug: {}
